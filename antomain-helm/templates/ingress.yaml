apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: antomain-ingress
spec:
  rules:
    - host: antomain.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: user-service
                port:
                  number: 80