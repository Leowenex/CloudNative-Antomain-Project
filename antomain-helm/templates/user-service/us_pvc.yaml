apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: user-service-pvc
  labels:
    app: Antomain
    service: user-service
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi